# this is a file that should be with out errors.
# NOTE We are not testing YML parsing.
# Just our wrapper feature

type: static-library
name: foobar
# This next line should start with a TAB
# If you edit this file with a 'smart editor' it will
# Pobably convert the tab to a space breaking the test
dogs:
   and_cats: true
  
